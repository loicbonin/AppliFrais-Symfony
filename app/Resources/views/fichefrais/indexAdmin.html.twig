{% extends 'baseAdmin.html.twig' %}

{% block body %}
    <div style="margin: 25px;" class="grey-text">
        <i class="material-icons left grey-text">people</i>Utilisateurs
        <div class="divider"></div>
    </div>
    <ul class="collapsible popout" data-collapsible="accordion">

            {% for user in users %}
        <li>
                <div class="collapsible-header">
                    <i class="material-icons blue-text text-darken-4">person</i>
                    {{ user.firstName }} {{ user.lastName }}
                </div>
            <div class="collapsible-body">
                <table class="bordered highlight">
                    <thead>
                    <tr>
                        <th>Matricule</th>
                        <th>Prénom</th>
                        <th>Nom</th>
                        <th>Fiches</th>

                    </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td>{{ user.id }}</td>
                            <td>{{ user.firstName }}</td>
                            <td>{{ user.lastName }}</td>
                            <td>
                                <a class="btn blue darken-4" href="{{ path('fiche_par_user', { 'id': user.id }) }}">Voir les fiches</a>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </li>
            {% endfor %}

    </ul>
{% endblock %}
