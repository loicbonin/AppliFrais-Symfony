{% extends 'base.html.twig' %}

{% block body %}
    <h1>Etat du forfait frais</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Edit" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('fichefrais_index') }}">Retour à la liste</a>
        </li>
    </ul>
{% endblock %}
